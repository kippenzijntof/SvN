@model IEnumerable<Contracts.Product>
@{
    ViewData["Title"] = "Product summary";
}

@foreach (var product in Model)
{
    <div>
        <h3>@product.Name</h3>
        @product.Description
        <h4>@product.Price.ToString("c")</h4>
    </div>
}

